# =============================================================================
# Instagram Reels Auto Uploader — Environment Variables
# =============================================================================
# Copy this file to .env and fill in all required values.
# NEVER commit your .env file to version control.
# =============================================================================

# ─── Server ──────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# API key to secure the internal endpoints (generate a strong random string)
API_KEY=your_strong_random_api_key_here

# The publicly accessible URL of this server (REQUIRED for Instagram API)
# e.g., https://your-domain.com or an ngrok URL
PUBLIC_URL=https://your-public-domain.com

# ─── Google Drive API ─────────────────────────────────────────────────────────
# Obtained from Google Cloud Console → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Refresh token obtained by running: node scripts/get-refresh-token.js
GOOGLE_REFRESH_TOKEN=your_google_refresh_token

# The ID of the "Videos" folder in Google Drive
# Found in the URL: drive.google.com/drive/folders/<FOLDER_ID>
GOOGLE_DRIVE_FOLDER_ID=your_videos_folder_id

# The ID of the "Uploaded" folder to move processed videos into
GOOGLE_DRIVE_UPLOADED_FOLDER_ID=your_uploaded_folder_id

# ─── Instagram / Meta Graph API ──────────────────────────────────────────────
# Your Instagram Business Account ID
INSTAGRAM_ACCOUNT_ID=your_instagram_business_account_id

# Your connected Facebook Page ID
FACEBOOK_PAGE_ID=your_facebook_page_id

# Long-lived access token from Meta for Developers
# See: docs/SETUP_INSTAGRAM.md
GRAPH_API_TOKEN=your_meta_graph_api_long_lived_token

# ─── Content Configuration ───────────────────────────────────────────────────
# Path to the text file containing the caption for all Reels
CAPTION_FILE=./caption.txt

# Path to the cover image (JPEG) to use for all Reels
# Can be an absolute path or relative to the project root
COVER_IMAGE=./public/cover/cover.jpg

# ─── Upload Settings ──────────────────────────────────────────────────────────
# Maximum video file size in bytes (default: 1GB)
MAX_FILE_SIZE_BYTES=1073741824

# Maximum video duration in seconds (default: 15 minutes)
MAX_DURATION_SECONDS=900

# How often to poll Google Drive for new videos (cron syntax)
# Default: */10 * * * * (every 10 minutes)
# Leave empty or set to false to disable internal polling (useful if using n8n exclusively)
POLLING_CRON=*/10 * * * *

# Delay in seconds between sequential uploads (STRICT — never upload two at once)
# Default: 120 seconds (2 minutes)
UPLOAD_DELAY_SECONDS=120

# Maximum retry attempts for failed uploads
MAX_RETRY_ATTEMPTS=3

# Base delay in ms between retries (doubles each attempt)
RETRY_BASE_DELAY_MS=5000

# How long to wait between Instagram status poll checks (ms)
STATUS_POLL_INTERVAL_MS=15000

# Maximum time to wait for Instagram processing (ms) — 30 minutes
STATUS_POLL_TIMEOUT_MS=1800000

# ─── Database ────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@localhost:5432/instagram_uploader?schema=public"

# ─── Telegram Notifications ──────────────────────────────────────────────────
# Create a bot via @BotFather and get the token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Your Telegram chat ID (or group/channel ID)
TELEGRAM_CHAT_ID=your_telegram_chat_id

# ─── Logging ─────────────────────────────────────────────────────────────────
LOG_LEVEL=info
LOG_DIR=./logs
